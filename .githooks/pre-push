#!/bin/bash
# Pre-push hook: run backend tests before pushing

echo "🔍 Running backend tests before push..."
cd backend
npm test
status=$?

if [ $status -ne 0 ]; then
  echo "❌ Tests failed. Push aborted."
  exit 1
fi

echo "✅ All tests passed. Proceeding with push."
exit 0
